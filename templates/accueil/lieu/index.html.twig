{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

{% endblock %}

{% block body %}
    <div class="row row-sm justify-content-center mt-5">
        <div class="col-10">
            <div class="card custom-card">
                <div class="card-header  p-3 tx-semibold tx-white bg-primary">
                    Bloc lieu
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-bordered border-bottom" id="basic-datatable">
                            <thead>
                            <tr>
                                <th class="text-center">Mini titre</th>
                                <th class="text-center">Lieu</th>
                                <th class="text-center">Adresse courte</th>
                                <th class="text-center">Action</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for index, a in articles %}
                                <tr>
                                    <th class="text-center">{{ a.miniTitre }}</th>
                                    <th>{{ a.lieu }}</th>
                                    <th>{{ a.miniadresse }}</th>
                                    <th class="text-center">
                                        <a href="{{ path('app_accueil_lieu_show', {'id' : a.id}) }}">
                                            <i class="fa fa-eye"></i>
                                        </a>
                                    </th>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>

                </div>
                {% if articles | length == 0 %}
                <div class="card-footer">
                    <a href="{{ path('app_accueil_lieu_new') }}" class="btn btn-primary float-end">Nouveau</a>
                </div>
                {% endif %}
            </div>
        </div> <!-- end col -->
    </div> <!-- end row -->

{% endblock %}

